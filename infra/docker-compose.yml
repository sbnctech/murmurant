# Murmurant Local Development Stack
#
# This file defines the services needed to run Murmurant locally:
#   - db: PostgreSQL database
#   - web: Next.js web application
#
# Usage:
#   docker compose -f infra/docker-compose.yml up -d
#
# Stop:
#   docker compose -f infra/docker-compose.yml down

services:
  # PostgreSQL Database
  db:
    image: postgres:15-alpine
    container_name: murmurant-db
    environment:
      POSTGRES_USER: murmurant
      POSTGRES_PASSWORD: murmurant
      POSTGRES_DB: murmurant_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U murmurant -d murmurant_dev"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Next.js Web Application
  web:
    build:
      context: ..
      dockerfile: infra/Dockerfile.web
    container_name: murmurant-web
    environment:
      DATABASE_URL: postgresql://murmurant:murmurant@db:5432/murmurant_dev
      NODE_ENV: development
    ports:
      - "3000:3000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      # Mount source code for hot-reload during development
      - ..:/app
      # Prevent overwriting container's node_modules
      - /app/node_modules

volumes:
  postgres_data:
